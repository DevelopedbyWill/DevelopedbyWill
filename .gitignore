# Source briefs and synthesis (contain identifying information — never push)
project-briefs/
synthesis-master.md

# macOS
.DS_Store

# Local brand-voice guidelines (internal, do not push)
.claude/

# Defense-in-depth — secrets and dependencies never belong in this repo
node_modules/
.env
.env.*
*.pem
*.key
